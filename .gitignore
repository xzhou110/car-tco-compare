# dependencies (production app, added later)
node_modules/
dist/
.vite/

# env / local
.env
.env.local
*.local

# editor / OS / local tooling
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*

# local build backups written by the listings scripts (the canonical data is
# listings.json) — keep them on disk but never commit/ship them to Pages
app/public/data/*.bak.json
